# WT-5 — Новости (News): BACKEND (новый модуль) + FRONT

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to execute this plan. Dispatch each `### Task N` to a fresh subagent in order; each task is TDD (write the failing test first, then the code, then make it green). Do NOT batch tasks. After every task, run the verification command in that task and confirm green output before moving on. Do NOT skip the `## Self-Review`.

## Goal

Заменить localStorage-хранилище новостей (`useStore` во фронте) полноценным backend-модулем `News` (NestJS + TypeORM + Postgres) с публичным чтением и админским CRUD под ролями. Перевести на этот API админ-вкладку и обе публичные страницы новостей.

## Architecture

- **Backend** — новый Nest-модуль `src/news/` по образцу `src/pricing/` (entity → migration → service+spec → controller с ролями → module → регистрация в `app.module.ts` и `src/config/data-source.ts`).
  - Публичное чтение: `GET /api/news`, `GET /api/news/:id` — помечены `@Public()` (в проекте глобальный `JwtAuthGuard` как `APP_GUARD`, поэтому без `@Public()` даже GET требует токен).
  - Админ CRUD: `POST /api/news`, `PUT /api/news/:id`, `DELETE /api/news/:id` — `@UseGuards(RolesGuard)` + `@Roles(UserRole.ADMIN, UserRole.MANAGER)`.
- **Front** — данные через `@tanstack/react-query`; HTTP только через `apiRequest<T>` (контракт WT-0). Слой `src/lib/api/newsApi.ts` инкапсулирует вызовы и маппинг backend-полей (`body/coverImage/publishedAt`) в существующую фронт-форму `NewsItem` (`content/image/date`), чтобы UI-страницы почти не менялись. localStorage-логика новостей удаляется из `src/store.ts`.

## Tech Stack

- Backend: NestJS 10, TypeORM 10, Postgres (`pg`), class-validator/class-transformer, Swagger. Тесты: Jest + ts-jest (`npm test`, паттерн `*.spec.ts`, `rootDir: src`).
- Front: React 19, react-router 7, @tanstack/react-query 5, zustand 5, Vite 6. Тест-раннера НЕТ — верификация фронта: `npm run lint` (это `tsc --noEmit`) + ручной прогон `npm run dev`.

## Global Constraints

- **Backend миграции (РЕАЛЬНЫЙ способ проекта):** таблицы создаются raw-SQL миграциями в `src/migrations/NNN-*.ts` (см. `1700000000013-CreateAppSettings.ts`). Раннер: `npm run migration:run` (использует `src/config/data-source.ts`). `synchronize` включён ТОЛЬКО при `NODE_ENV=development` в `app.module.ts`; production полагается на миграции. Поэтому новую сущность нужно добавить в ДВА массива `entities`: `src/app.module.ts` И `src/config/data-source.ts`.
- Timestamp-имя новой миграции: `1700000000020` (следующий свободный номер после `...019-CreateCatalogCache`).
- Не менять `apiRequest` и `src/lib/api/index.ts` (index уже реэкспортит `newsApi` — по контракту WT-0). Импорт HTTP: `import { apiRequest } from '../../api';` (в этом фронте единый клиент лежит в `src/api.ts`; проверь фактический путь `src/http.ts` — на текущий момент его нет, `apiRequest` экспортируется из `src/api.ts`). **Если WT-0 создал `src/http.ts` — импортируй оттуда; иначе из `src/api.ts`.**
- Токен во фронте: `useAuthStore(s => s.accessToken)`.
- **MERGE-POINT:** единственный файл, потенциально общий с WT-6/WT-7 — `src/app.module.ts` (регистрация модуля в массивах `imports` и `entities`). Ожидается тривиальный merge-конфликт по добавляемым строкам; разрешать простым объединением импортов. `src/config/data-source.ts` также правится, но он вне зоны WT-6/WT-7.
- Backend-контракт ответа новости (единый JSON): `{ id: string, title: string, body: string, coverImage: string | null, publishedAt: string, createdAt: string, updatedAt: string }`. Даты — ISO-строки.

---

# BACKEND

## Task 1 — News entity

**Files**
- Create `src/news/entities/news.entity.ts`

**Interfaces**
```ts
class News {
  id: string;            // uuid PK
  title: string;         // varchar(255)
  body: string;          // text (HTML разрешён — отдаётся как есть)
  coverImage: string | null; // varchar(1024), nullable — URL обложки
  publishedAt: Date;     // timestamp, дефолт now()
  createdAt: Date;
  updatedAt: Date;
}
```

**Steps (TDD-lite: entity проверяется компиляцией + миграционным smoke в Task 3)**
- [ ] Создать файл со следующим содержимым:
```ts
import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, UpdateDateColumn } from 'typeorm';
import { ApiProperty } from '@nestjs/swagger';

@Entity('news')
export class News {
  @ApiProperty({ example: 'b3f1...uuid' })
  @PrimaryGeneratedColumn('uuid')
  id: string;

  @ApiProperty({ example: 'Появились запчасти на китайские авто' })
  @Column({ length: 255 })
  title: string;

  @ApiProperty({ example: '<b>Огромный ассортимент</b> деталей...', description: 'HTML-контент новости' })
  @Column({ type: 'text' })
  body: string;

  @ApiProperty({ example: 'https://.../cover.jpg', nullable: true })
  @Column({ type: 'varchar', length: 1024, nullable: true, default: null })
  coverImage: string | null;

  @ApiProperty({ example: '2026-07-03T10:00:00.000Z' })
  @Column({ type: 'timestamp', default: () => 'now()' })
  publishedAt: Date;

  @CreateDateColumn() createdAt: Date;
  @UpdateDateColumn() updatedAt: Date;
}
```
- [ ] Verify: `npx tsc --noEmit` (в корне backend) — файл компилируется.

---

## Task 2 — DTOs (Create/Update)

**Files**
- Create `src/news/dto/create-news.dto.ts`
- Create `src/news/dto/update-news.dto.ts`

**Interfaces**
```ts
class CreateNewsDto { title: string; body: string; coverImage?: string | null; publishedAt?: string; }
class UpdateNewsDto { title?: string; body?: string; coverImage?: string | null; publishedAt?: string; }
```

**Steps**
- [ ] `create-news.dto.ts`:
```ts
import { IsISO8601, IsOptional, IsString, MaxLength, MinLength, ValidateIf } from 'class-validator';
import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';

export class CreateNewsDto {
  @ApiProperty({ example: 'Появились запчасти на китайские авто' })
  @IsString() @MinLength(1) @MaxLength(255)
  title: string;

  @ApiProperty({ example: '<b>Огромный ассортимент</b> деталей...' })
  @IsString() @MinLength(1)
  body: string;

  @ApiPropertyOptional({ example: 'https://.../cover.jpg', nullable: true })
  @IsOptional() @ValidateIf((_, v) => v !== null) @IsString() @MaxLength(1024)
  coverImage?: string | null;

  @ApiPropertyOptional({ example: '2026-07-03T10:00:00.000Z', description: 'ISO-дата публикации; по умолчанию — текущее время' })
  @IsOptional() @IsISO8601()
  publishedAt?: string;
}
```
- [ ] `update-news.dto.ts`:
```ts
import { IsISO8601, IsOptional, IsString, MaxLength, MinLength, ValidateIf } from 'class-validator';
import { ApiPropertyOptional } from '@nestjs/swagger';

export class UpdateNewsDto {
  @ApiPropertyOptional({ example: 'Новый заголовок' })
  @IsOptional() @IsString() @MinLength(1) @MaxLength(255)
  title?: string;

  @ApiPropertyOptional({ example: '<i>обновлённый контент</i>' })
  @IsOptional() @IsString() @MinLength(1)
  body?: string;

  @ApiPropertyOptional({ example: 'https://.../cover.jpg', nullable: true })
  @IsOptional() @ValidateIf((_, v) => v !== null) @IsString() @MaxLength(1024)
  coverImage?: string | null;

  @ApiPropertyOptional({ example: '2026-07-03T10:00:00.000Z' })
  @IsOptional() @IsISO8601()
  publishedAt?: string;
}
```
- [ ] Verify: `npx tsc --noEmit`.

---

## Task 3 — Migration `CreateNews`

**Files**
- Create `src/migrations/1700000000020-CreateNews.ts`
- Edit `src/config/data-source.ts` (добавить `News` в `entities`)

**Steps**
- [ ] Создать миграцию (raw SQL, как в проекте):
```ts
import { MigrationInterface, QueryRunner } from 'typeorm';

export class CreateNews1700000000020 implements MigrationInterface {
  name = 'CreateNews1700000000020';

  public async up(q: QueryRunner): Promise<void> {
    await q.query(`
      CREATE TABLE "news" (
        "id" uuid NOT NULL DEFAULT uuid_generate_v4(),
        "title" character varying(255) NOT NULL,
        "body" text NOT NULL,
        "coverImage" character varying(1024),
        "publishedAt" TIMESTAMP NOT NULL DEFAULT now(),
        "createdAt" TIMESTAMP NOT NULL DEFAULT now(),
        "updatedAt" TIMESTAMP NOT NULL DEFAULT now(),
        CONSTRAINT "PK_news" PRIMARY KEY ("id")
      )
    `);
    await q.query(`CREATE INDEX "IDX_news_publishedAt" ON "news" ("publishedAt")`);
  }

  public async down(q: QueryRunner): Promise<void> {
    await q.query(`DROP INDEX "IDX_news_publishedAt"`);
    await q.query(`DROP TABLE "news"`);
  }
}
```
  > ПРИМЕЧАНИЕ про `uuid_generate_v4()`: убедись, что расширение `uuid-ossp` включено (в проекте другие uuid-таблицы уже создаются TypeORM'ом). Проверь предыдущую миграцию `1700000000000-CreateUsersTable.ts`: если там uuid генерируется через `uuid_generate_v4()` без явного `CREATE EXTENSION`, значит расширение уже есть — оставь как есть. Если users использует `gen_random_uuid()` (pgcrypto), ИСПОЛЬЗУЙ `gen_random_uuid()` здесь для единообразия. **Свериться обязательно перед запуском.**
- [ ] Добавить `News` в `src/config/data-source.ts`:
  - импорт: `import { News } from '../news/entities/news.entity';`
  - в массив `entities: [ ... , News ]`.
- [ ] Verify (требуется поднятая dev-БД):
  - `npm run migration:run` — миграция применяется без ошибок.
  - `npm run migration:revert` затем снова `npm run migration:run` — up/down обратимы.
  - Если БД недоступна в среде агента — задокументируй это и ограничься `npx tsc --noEmit` для проверки миграции на компиляцию; отметь пункт как «требует ручного прогона миграции».

---

## Task 4 — NewsService + unit-spec (TDD)

**Files**
- Create `src/news/news.service.spec.ts` (СНАЧАЛА)
- Create `src/news/news.service.ts`

**Interfaces**
```ts
class NewsService {
  findAll(): Promise<News[]>;                       // сортировка publishedAt DESC
  findOne(id: string): Promise<News>;               // NotFoundException если нет
  create(dto: CreateNewsDto): Promise<News>;        // publishedAt по умолчанию now()
  update(id: string, dto: UpdateNewsDto): Promise<News>; // NotFoundException если нет
  remove(id: string): Promise<void>;                // NotFoundException если affected === 0
}
```

**Steps**
- [ ] Написать СНАЧАЛА `news.service.spec.ts` (в стиле `brand-markup.service.spec.ts` — конструирование сервиса с мок-репозиторием, без TestingModule):
```ts
import { NotFoundException } from '@nestjs/common';
import { NewsService } from './news.service';

function make(rows: any[] = []) {
  const repo = {
    find: jest.fn(async () => rows),
    findOne: jest.fn(async ({ where: { id } }: any) => rows.find((r) => r.id === id) ?? null),
    create: jest.fn((data: any) => ({ ...data })),
    save: jest.fn(async (r: any) => ({ id: r.id ?? 'generated-id', ...r })),
    delete: jest.fn(async (id: string) => ({ affected: rows.some((r) => r.id === id) ? 1 : 0 })),
  };
  return { svc: new NewsService(repo as any), repo };
}

describe('NewsService', () => {
  it('findAll sorts by publishedAt DESC', async () => {
    const { svc, repo } = make([{ id: '1' }]);
    await svc.findAll();
    expect(repo.find).toHaveBeenCalledWith({ order: { publishedAt: 'DESC' } });
  });

  it('findOne throws NotFoundException when missing', async () => {
    const { svc } = make([]);
    await expect(svc.findOne('nope')).rejects.toBeInstanceOf(NotFoundException);
  });

  it('findOne returns the row when present', async () => {
    const { svc } = make([{ id: 'a', title: 'T' }]);
    expect(await svc.findOne('a')).toEqual({ id: 'a', title: 'T' });
  });

  it('create defaults publishedAt to now() when omitted', async () => {
    const { svc, repo } = make([]);
    const before = Date.now();
    const res = await svc.create({ title: 'X', body: 'B' } as any);
    const after = Date.now();
    const savedArg = repo.save.mock.calls[0][0];
    expect(savedArg.publishedAt).toBeInstanceOf(Date);
    expect(savedArg.publishedAt.getTime()).toBeGreaterThanOrEqual(before);
    expect(savedArg.publishedAt.getTime()).toBeLessThanOrEqual(after);
    expect(res.title).toBe('X');
  });

  it('create uses provided publishedAt (ISO string -> Date)', async () => {
    const { svc, repo } = make([]);
    await svc.create({ title: 'X', body: 'B', publishedAt: '2026-01-01T00:00:00.000Z' } as any);
    const savedArg = repo.save.mock.calls[0][0];
    expect(savedArg.publishedAt.toISOString()).toBe('2026-01-01T00:00:00.000Z');
  });

  it('create defaults coverImage to null when omitted', async () => {
    const { svc, repo } = make([]);
    await svc.create({ title: 'X', body: 'B' } as any);
    expect(repo.save.mock.calls[0][0].coverImage).toBeNull();
  });

  it('update throws NotFoundException when missing', async () => {
    const { svc } = make([]);
    await expect(svc.update('nope', { title: 'Y' })).rejects.toBeInstanceOf(NotFoundException);
  });

  it('update merges only provided fields', async () => {
    const { svc, repo } = make([{ id: 'a', title: 'Old', body: 'B', coverImage: null }]);
    await svc.update('a', { title: 'New' });
    const savedArg = repo.save.mock.calls[0][0];
    expect(savedArg.title).toBe('New');
    expect(savedArg.body).toBe('B');
  });

  it('remove throws NotFoundException when nothing deleted', async () => {
    const { svc } = make([]);
    await expect(svc.remove('nope')).rejects.toBeInstanceOf(NotFoundException);
  });

  it('remove resolves when a row is deleted', async () => {
    const { svc } = make([{ id: 'a' }]);
    await expect(svc.remove('a')).resolves.toBeUndefined();
  });
});
```
- [ ] Запустить `npm test -- news.service` → тесты падают (нет реализации). Это ожидаемый RED.
- [ ] Написать `news.service.ts` до зелёного:
```ts
import { Injectable, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { News } from './entities/news.entity';
import { CreateNewsDto } from './dto/create-news.dto';
import { UpdateNewsDto } from './dto/update-news.dto';

@Injectable()
export class NewsService {
  constructor(
    @InjectRepository(News)
    private readonly repo: Repository<News>,
  ) {}

  findAll(): Promise<News[]> {
    return this.repo.find({ order: { publishedAt: 'DESC' } });
  }

  async findOne(id: string): Promise<News> {
    const row = await this.repo.findOne({ where: { id } });
    if (!row) throw new NotFoundException('News item not found.');
    return row;
  }

  create(dto: CreateNewsDto): Promise<News> {
    const entity = this.repo.create({
      title: dto.title,
      body: dto.body,
      coverImage: dto.coverImage ?? null,
      publishedAt: dto.publishedAt ? new Date(dto.publishedAt) : new Date(),
    });
    return this.repo.save(entity);
  }

  async update(id: string, dto: UpdateNewsDto): Promise<News> {
    const row = await this.findOne(id);
    if (dto.title !== undefined) row.title = dto.title;
    if (dto.body !== undefined) row.body = dto.body;
    if (dto.coverImage !== undefined) row.coverImage = dto.coverImage;
    if (dto.publishedAt !== undefined) row.publishedAt = new Date(dto.publishedAt);
    return this.repo.save(row);
  }

  async remove(id: string): Promise<void> {
    const res = await this.repo.delete(id);
    if (!res.affected) throw new NotFoundException('News item not found.');
  }
}
```
- [ ] Verify: `npm test -- news.service` → все зелёные.

---

## Task 5 — NewsController (public GET + admin CRUD под ролями)

**Files**
- Create `src/news/news.controller.ts`

**Interfaces (маршруты)**
- `@Public() GET /api/news` → `News[]`
- `@Public() GET /api/news/:id` → `News` (404 если нет)
- `@Roles(ADMIN, MANAGER) POST /api/news` (body `CreateNewsDto`) → `News` (201)
- `@Roles(ADMIN, MANAGER) PUT /api/news/:id` (body `UpdateNewsDto`) → `News`
- `@Roles(ADMIN, MANAGER) DELETE /api/news/:id` → `204/void`

**Steps**
- [ ] Создать контроллер (образец — `brand-markup.controller.ts`; `@Public()` из `src/auth/decorators/public.decorator.ts` нужен, т.к. `JwtAuthGuard` глобален):
```ts
import {
  Body, Controller, Delete, Get, HttpCode, Param, ParseUUIDPipe, Post, Put, UseGuards,
} from '@nestjs/common';
import { ApiBearerAuth, ApiOperation, ApiTags } from '@nestjs/swagger';
import { Public } from '../auth/decorators/public.decorator';
import { RolesGuard } from '../auth/guards/roles.guard';
import { Roles } from '../auth/decorators/roles.decorator';
import { UserRole } from '../users/entities/user.entity';
import { NewsService } from './news.service';
import { CreateNewsDto } from './dto/create-news.dto';
import { UpdateNewsDto } from './dto/update-news.dto';

@ApiTags('news')
@Controller('news')
export class NewsController {
  constructor(private readonly service: NewsService) {}

  @Public()
  @Get()
  @ApiOperation({ summary: 'List news, newest first (public)' })
  findAll() {
    return this.service.findAll();
  }

  @Public()
  @Get(':id')
  @ApiOperation({ summary: 'Get one news item (public)' })
  findOne(@Param('id', ParseUUIDPipe) id: string) {
    return this.service.findOne(id);
  }

  @ApiBearerAuth()
  @UseGuards(RolesGuard)
  @Roles(UserRole.ADMIN, UserRole.MANAGER)
  @Post()
  @ApiOperation({ summary: 'Create news (ADMIN/MANAGER)' })
  create(@Body() dto: CreateNewsDto) {
    return this.service.create(dto);
  }

  @ApiBearerAuth()
  @UseGuards(RolesGuard)
  @Roles(UserRole.ADMIN, UserRole.MANAGER)
  @Put(':id')
  @ApiOperation({ summary: 'Update news (ADMIN/MANAGER)' })
  update(@Param('id', ParseUUIDPipe) id: string, @Body() dto: UpdateNewsDto) {
    return this.service.update(id, dto);
  }

  @ApiBearerAuth()
  @UseGuards(RolesGuard)
  @Roles(UserRole.ADMIN, UserRole.MANAGER)
  @Delete(':id')
  @HttpCode(204)
  @ApiOperation({ summary: 'Delete news (ADMIN/MANAGER)' })
  remove(@Param('id', ParseUUIDPipe) id: string) {
    return this.service.remove(id);
  }
}
```
- [ ] Verify: `npx tsc --noEmit`.

---

## Task 6 — NewsModule + регистрация в app.module.ts (MERGE-POINT)

**Files**
- Create `src/news/news.module.ts`
- Edit `src/app.module.ts` (импорт модуля + сущности в двух местах)

**Steps**
- [ ] `news.module.ts` (образец — `pricing.module.ts`):
```ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { News } from './entities/news.entity';
import { NewsService } from './news.service';
import { NewsController } from './news.controller';

@Module({
  imports: [TypeOrmModule.forFeature([News])],
  controllers: [NewsController],
  providers: [NewsService],
  exports: [NewsService],
})
export class NewsModule {}
```
- [ ] В `src/app.module.ts` (⚠ MERGE-POINT с WT-6/WT-7 — разрешай объединением строк):
  - добавить импорты:
    ```ts
    import { NewsModule } from './news/news.module';
    import { News } from './news/entities/news.entity';
    ```
  - добавить `News` в массив `entities: [ ... , CatalogCache, News ]` внутри `TypeOrmModule.forRootAsync`.
  - добавить `NewsModule` в массив `imports: [ ... , CatalogModule, NewsModule ]`.
- [ ] Verify:
  - `npx tsc --noEmit`
  - `npm test` — весь backend-набор зелёный.
  - (если БД доступна) `npm run start:dev`, затем `curl -s localhost:3000/api/news` → `[]` или список; `curl` POST без токена → 401/403; `POST` с админ-токеном → 201.

---

# FRONT

> Порядок: сначала `newsApi.ts` (слой данных), затем UI. Тест-раннера нет — после каждой задачи `npm run lint` (= `tsc --noEmit`); в конце ручной прогон `npm run dev`.

## Task 7 — `src/lib/api/newsApi.ts` (слой данных + маппинг)

**Files**
- Edit `src/lib/api/newsApi.ts` (в WT-0 пустой; здесь наполняем)
- НЕ трогать `src/lib/api/index.ts` (уже реэкспортит `newsApi`).

**Контекст импорта HTTP:** на текущий момент `apiRequest` экспортируется из `src/api.ts`. Контракт WT-0 предписывает `import { apiRequest } from '../http';`. **Перед написанием проверь фактическое наличие `src/http.ts`:** если он есть — импортируй `from '../../http'` (файл лежит в `src/lib/api/`, до `src/` два уровня вверх). Если `src/http.ts` отсутствует — импортируй `from '../../api'`. Зафиксируй фактический выбор в коде.

**Interfaces**
```ts
// Backend-форма (как отдаёт /api/news)
export interface NewsDto {
  id: string; title: string; body: string;
  coverImage: string | null;
  publishedAt: string; createdAt: string; updatedAt: string;
}
// Фронт-форма, потребляемая страницами (совместима со старым NewsItem)
export interface NewsItem { id: string; title: string; content: string; image: string; date: string; }
// Вход для создания/редактирования из админки
export interface NewsInput { title: string; content: string; image: string; publishedAt?: string; }

export const newsApi: {
  list(): Promise<NewsItem[]>;
  get(id: string): Promise<NewsItem>;
  create(token: string, input: NewsInput): Promise<NewsItem>;
  update(token: string, id: string, input: NewsInput): Promise<NewsItem>;
  remove(token: string, id: string): Promise<void>;
};
```

**Steps**
- [ ] Написать файл:
```ts
import { apiRequest } from '../../api'; // ← если WT-0 создал src/http.ts, заменить на '../../http'

export interface NewsDto {
  id: string;
  title: string;
  body: string;
  coverImage: string | null;
  publishedAt: string;
  createdAt: string;
  updatedAt: string;
}

export interface NewsItem {
  id: string;
  title: string;
  content: string;
  image: string;
  date: string;
}

export interface NewsInput {
  title: string;
  content: string;
  image: string;
  publishedAt?: string;
}

const toItem = (dto: NewsDto): NewsItem => ({
  id: dto.id,
  title: dto.title,
  content: dto.body,
  image: dto.coverImage ?? '',
  date: dto.publishedAt,
});

const toBody = (input: NewsInput) => ({
  title: input.title,
  body: input.content,
  coverImage: input.image ? input.image : null,
  ...(input.publishedAt ? { publishedAt: input.publishedAt } : {}),
});

export const newsApi = {
  list: async (): Promise<NewsItem[]> => {
    const rows = await apiRequest<NewsDto[]>('/api/news', { method: 'GET' });
    return rows.map(toItem);
  },

  get: async (id: string): Promise<NewsItem> => {
    const row = await apiRequest<NewsDto>(`/api/news/${id}`, { method: 'GET' });
    return toItem(row);
  },

  create: async (token: string, input: NewsInput): Promise<NewsItem> => {
    const row = await apiRequest<NewsDto>('/api/news', { method: 'POST', token, body: toBody(input) });
    return toItem(row);
  },

  update: async (token: string, id: string, input: NewsInput): Promise<NewsItem> => {
    const row = await apiRequest<NewsDto>(`/api/news/${id}`, { method: 'PUT', token, body: toBody(input) });
    return toItem(row);
  },

  remove: async (token: string, id: string): Promise<void> => {
    await apiRequest<void>(`/api/news/${id}`, { method: 'DELETE', token });
  },
};
```
- [ ] Verify: `npm run lint`.

---

## Task 8 — `NewsTab` на react-query (админ CRUD)

**Files**
- Edit `src/pages/admin/tabs/NewsTab.tsx` (создан WT-0 как `export const NewsTab: React.FC`; сюда переносим `renderNews` из `Admin.tsx` и переводим на API).
- Удалить импорт/использование `useStore` news-логики внутри этого файла.

**Контекст:** оригинальный UI — в `src/pages/Admin.tsx` (`renderNews`, ~стр.517-643): форма редактирования (title, image-URL + превью, textarea content с HTML-подсказкой) + таблица (Фото / Заголовок / Дата / Действия) с кнопками Edit/Delete и кнопкой «Добавить новость». Сохранить эту вёрстку 1:1, заменив источник данных.

**Interfaces**
```ts
export const NewsTab: React.FC; // без пропсов; токен берёт из useAuthStore
```

**Steps**
- [ ] Реализовать `NewsTab` с react-query. Полный код:
```tsx
import React, { useState } from 'react';
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { Plus, Save, ArrowLeft, Edit, Trash2 } from 'lucide-react';
import { newsApi, type NewsItem, type NewsInput } from '../../../lib/api';
import { useAuthStore } from '../../../authStore';

type Draft = { id?: string; title: string; content: string; image: string; date?: string };

const NEWS_KEY = ['news'] as const;

export const NewsTab: React.FC = () => {
  const token = useAuthStore((s) => s.accessToken);
  const queryClient = useQueryClient();
  const [editing, setEditing] = useState<Draft | null>(null);

  const { data: news = [], isLoading, isError } = useQuery({
    queryKey: NEWS_KEY,
    queryFn: () => newsApi.list(),
  });

  const invalidate = () => queryClient.invalidateQueries({ queryKey: NEWS_KEY });

  const saveMutation = useMutation({
    mutationFn: (draft: Draft) => {
      if (!token) throw new Error('Нет токена авторизации.');
      const input: NewsInput = {
        title: draft.title,
        content: draft.content,
        image: draft.image,
        ...(draft.date ? { publishedAt: draft.date } : {}),
      };
      return draft.id ? newsApi.update(token, draft.id, input) : newsApi.create(token, input);
    },
    onSuccess: () => {
      invalidate();
      setEditing(null);
    },
  });

  const deleteMutation = useMutation({
    mutationFn: (id: string) => {
      if (!token) throw new Error('Нет токена авторизации.');
      return newsApi.remove(token, id);
    },
    onSuccess: invalidate,
  });

  if (editing) {
    return (
      <div>
        <div className="flex items-center gap-4 mb-6">
          <button onClick={() => setEditing(null)} className="text-slate-400 hover:text-slate-900"><ArrowLeft size={20} /></button>
          <h2 className="text-[18px] font-bold text-slate-900 border-l-4 border-orange-500 pl-3">
            {editing.id ? 'Редактировать новость' : 'Создать новость'}
          </h2>
        </div>
        <div className="bg-white border border-slate-200 rounded-lg p-6 max-w-3xl">
          <div className="space-y-4">
            <div>
              <label className="block text-[13px] font-bold text-slate-700 mb-1">Заголовок</label>
              <input
                type="text"
                value={editing.title}
                onChange={(e) => setEditing({ ...editing, title: e.target.value })}
                className="w-full border border-slate-200 rounded p-2.5 outline-none focus:border-orange-500 text-[14px]"
                placeholder="Введите заголовок"
              />
            </div>
            <div>
              <label className="block text-[13px] font-bold text-slate-700 mb-1">URL Картинки (Загрузка)</label>
              <input
                type="text"
                value={editing.image}
                onChange={(e) => setEditing({ ...editing, image: e.target.value })}
                className="w-full border border-slate-200 rounded p-2.5 outline-none focus:border-orange-500 text-[14px]"
                placeholder="https://images.unsplash.com/..."
              />
              {editing.image && (
                <div className="mt-2 h-32 rounded bg-slate-100 overflow-hidden border border-slate-200">
                  <img src={editing.image} alt="Preview" className="h-full object-cover" />
                </div>
              )}
            </div>
            <div>
              <div className="flex justify-between items-end mb-1">
                <label className="block text-[13px] font-bold text-slate-700">Описание (HTML поддерживается)</label>
                <div className="flex gap-1 text-[11px] font-mono text-slate-500 bg-slate-100 p-1 rounded">
                  <span>&lt;b&gt;жирный&lt;/b&gt;</span>
                  <span>&lt;i&gt;курсив&lt;/i&gt;</span>
                  <span>&lt;h3&gt;заголовок&lt;/h3&gt;</span>
                </div>
              </div>
              <textarea
                value={editing.content}
                onChange={(e) => setEditing({ ...editing, content: e.target.value })}
                className="w-full border border-slate-200 rounded p-2.5 outline-none focus:border-orange-500 text-[14px] h-40"
                placeholder="Описание новости..."
              />
            </div>
            {saveMutation.isError && (
              <div className="text-[13px] text-red-500">Не удалось сохранить новость.</div>
            )}
            <div className="pt-4 flex flex-col sm:flex-row gap-3">
              <button
                onClick={() => saveMutation.mutate(editing)}
                disabled={saveMutation.isPending || !editing.title.trim() || !editing.content.trim()}
                className="w-full sm:w-auto justify-center bg-orange-500 hover:bg-orange-600 disabled:opacity-50 text-white px-6 py-2.5 rounded text-[13px] font-bold flex items-center gap-2"
              >
                <Save size={16} /> {saveMutation.isPending ? 'Сохранение...' : 'Сохранить'}
              </button>
              <button onClick={() => setEditing(null)} className="w-full sm:w-auto justify-center bg-slate-100 hover:bg-slate-200 text-slate-700 px-6 py-2.5 rounded text-[13px] font-bold">
                Отмена
              </button>
            </div>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div>
      <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
        <h2 className="text-[18px] font-bold text-slate-900 border-l-4 border-orange-500 pl-3">Лента новостей</h2>
        <button
          onClick={() => setEditing({ title: '', content: '', image: '' })}
          className="bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 rounded text-[13px] font-bold flex items-center gap-2"
        >
          <Plus size={16} /> Добавить новость
        </button>
      </div>

      <div className="bg-white border border-slate-200 rounded-lg overflow-hidden overflow-x-auto">
        <table className="w-full min-w-[500px] text-left text-[13px]">
          <thead className="bg-slate-50 border-b border-slate-200 text-slate-500 uppercase tracking-wider text-[11px]">
            <tr>
              <th className="p-4 font-bold">Фото</th>
              <th className="p-4 font-bold w-1/3">Заголовок</th>
              <th className="p-4 font-bold">Дата</th>
              <th className="p-4 font-bold text-right">Действия</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-slate-200">
            {news.map((item: NewsItem) => (
              <tr key={item.id} className="hover:bg-slate-50">
                <td className="p-4">
                  <div className="w-16 h-10 rounded overflow-hidden bg-slate-100">
                    {item.image && <img src={item.image} alt={item.title} className="w-full h-full object-cover" />}
                  </div>
                </td>
                <td className="p-4 font-bold text-slate-900">{item.title}</td>
                <td className="p-4 text-slate-500">{new Date(item.date).toLocaleDateString('ru-RU')}</td>
                <td className="p-4 text-right">
                  <button
                    onClick={() => setEditing({ id: item.id, title: item.title, content: item.content, image: item.image, date: item.date })}
                    className="text-slate-400 hover:text-blue-500 p-2"
                  >
                    <Edit size={16} />
                  </button>
                  <button
                    onClick={() => { if (confirm('Удалить новость?')) deleteMutation.mutate(item.id); }}
                    className="text-slate-400 hover:text-red-500 p-2"
                  >
                    <Trash2 size={16} />
                  </button>
                </td>
              </tr>
            ))}
            {isLoading && (
              <tr><td colSpan={4} className="p-8 text-center text-slate-500">Загрузка...</td></tr>
            )}
            {isError && !isLoading && (
              <tr><td colSpan={4} className="p-8 text-center text-red-500">Не удалось загрузить новости.</td></tr>
            )}
            {!isLoading && !isError && news.length === 0 && (
              <tr><td colSpan={4} className="p-8 text-center text-slate-500">Лента пуста. Добавьте первую новость.</td></tr>
            )}
          </tbody>
        </table>
      </div>
    </div>
  );
};
```
  > Уточнить фактические пути импортов: `authStore` в этом фронте лежит в `src/authStore.ts`, из `src/pages/admin/tabs/` это `../../../authStore`. `lib/api` — `../../../lib/api`. Свериться перед финализацией.
- [ ] Если после WT-0 в `Admin.tsx` остался старый `renderNews`/локальный `editingNews`/импорты `useStore` news — они должны быть удалены WT-0 (вкладка вынесена). Если НЕ удалены (WT-0 ещё не смёржен в эту ветку) — убрать news-специфичный мёртвый код из `Admin.tsx` в рамках Task 10.
- [ ] Verify: `npm run lint`.

---

## Task 9 — Публичные страницы `NewsList` и `NewsArticle` на API

**Files**
- Edit `src/pages/NewsList.tsx`
- Edit `src/pages/NewsArticle.tsx`

**Steps**
- [ ] `NewsList.tsx` — заменить `useStore(state => state.news)` на react-query. Полный код:
```tsx
import React from 'react';
import { Link } from 'react-router-dom';
import { useQuery } from '@tanstack/react-query';
import { ArrowLeft, Calendar } from 'lucide-react';
import { newsApi } from '../lib/api';

export const NewsList: React.FC = () => {
  const { data: news = [], isLoading, isError } = useQuery({
    queryKey: ['news'],
    queryFn: () => newsApi.list(),
  });

  return (
    <div className="bg-slate-50 min-h-screen py-8">
      <div className="max-w-5xl mx-auto px-4 md:px-6">
        <div className="mb-8">
          <Link to="/" className="text-[13px] font-bold text-orange-500 hover:underline flex items-center gap-2 mb-4">
            <ArrowLeft size={16} /> На главную
          </Link>
          <h1 className="text-[28px] font-bold text-slate-900 border-l-4 border-orange-500 pl-4 py-1">Все новости</h1>
        </div>
        <div className="space-y-4">
          {news.map((item) => (
            <Link key={item.id} to={`/news/${item.id}`} className="bg-white border border-slate-200 rounded-xl p-4 flex flex-col sm:flex-row gap-5 hover:border-orange-500 hover:shadow-sm transition-all group">
              <div className="w-full sm:w-56 h-36 shrink-0 rounded-lg overflow-hidden">
                <img src={item.image} alt={item.title} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />
              </div>
              <div className="flex flex-col justify-center py-1">
                <div className="text-[11px] text-slate-400 font-bold mb-2 uppercase tracking-wide flex items-center gap-1.5">
                  <Calendar size={12} />
                  {new Date(item.date).toLocaleDateString('ru-RU')}
                </div>
                <h2 className="text-[18px] font-bold text-slate-900 mb-2 group-hover:text-orange-600 transition-colors">{item.title}</h2>
                <div className="text-[13px] text-slate-500 line-clamp-2 leading-relaxed" dangerouslySetInnerHTML={{ __html: item.content }} />
              </div>
            </Link>
          ))}
          {isLoading && (
            <div className="text-center py-16 bg-white border border-slate-200 rounded-xl text-slate-500">Загрузка...</div>
          )}
          {isError && !isLoading && (
            <div className="text-center py-16 bg-white border border-slate-200 rounded-xl text-red-500">Не удалось загрузить новости.</div>
          )}
          {!isLoading && !isError && news.length === 0 && (
            <div className="text-center py-16 bg-white border border-slate-200 rounded-xl text-slate-500">Нет доступных новостей.</div>
          )}
        </div>
      </div>
    </div>
  );
};
```
- [ ] `NewsArticle.tsx` — заменить поиск по стору на `newsApi.get(id)`. Полный код:
```tsx
import React from 'react';
import { useParams, Link, useNavigate } from 'react-router-dom';
import { useQuery } from '@tanstack/react-query';
import { ArrowLeft, Calendar } from 'lucide-react';
import { newsApi } from '../lib/api';

export const NewsArticle: React.FC = () => {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();

  const { data: article, isLoading, isError } = useQuery({
    queryKey: ['news', id],
    queryFn: () => newsApi.get(id as string),
    enabled: !!id,
  });

  if (isLoading) {
    return (
      <div className="min-h-[50vh] flex items-center justify-center text-slate-500">Загрузка...</div>
    );
  }

  if (isError || !article) {
    return (
      <div className="min-h-[50vh] flex flex-col items-center justify-center p-6 text-center">
        <h2 className="text-2xl font-bold mb-4 text-slate-800">Новость не найдена</h2>
        <p className="text-slate-500 mb-6">Возможно, ссылка была удалена или изменена.</p>
        <button onClick={() => navigate('/news')} className="bg-orange-500 hover:bg-orange-600 text-white font-bold py-2 px-6 rounded transition-colors">
          Вернуться к новостям
        </button>
      </div>
    );
  }

  return (
    <div className="bg-slate-50 min-h-screen py-8">
      <div className="max-w-3xl mx-auto px-4 md:px-6">
        <Link to="/news" className="text-[13px] font-bold text-orange-500 hover:underline flex items-center gap-2 mb-6">
          <ArrowLeft size={16} /> К списку новостей
        </Link>

        <article className="bg-white border border-slate-200 rounded-2xl overflow-hidden shadow-sm">
          <div className="w-full h-64 sm:h-80 md:h-[400px]">
            <img src={article.image} alt={article.title} className="w-full h-full object-cover" />
          </div>
          <div className="p-6 md:p-10">
            <div className="flex items-center gap-1.5 text-[12px] text-slate-400 font-bold mb-4 uppercase tracking-wider">
              <Calendar size={14} />
              {new Date(article.date).toLocaleDateString('ru-RU')}
            </div>
            <h1 className="text-[26px] md:text-[34px] font-extrabold text-slate-900 mb-8 leading-tight">
              {article.title}
            </h1>
            <div
              className="prose max-w-none text-slate-700 text-[15px] leading-relaxed"
              dangerouslySetInnerHTML={{ __html: article.content }}
            />
          </div>
        </article>
      </div>
    </div>
  );
};
```
- [ ] Verify: `npm run lint`.

---

## Task 10 — Удалить news-логику из `store.ts` (localStorage)

**Files**
- Edit `src/store.ts`
- (при необходимости) Edit `src/pages/Admin.tsx` — убрать мёртвый news-код, если WT-0 его не удалил.

**Steps**
- [ ] Из `src/store.ts` удалить: интерфейс `NewsItem`, поля `news`, `addNews`, `updateNews`, `deleteNews` из `StoreState` и из реализации стора (включая моковый массив `news: [...]`). Остальной стор (`favorites`, `city`) не трогать.
  > ⚠ `NewsItem` теперь живёт в `src/lib/api/newsApi.ts` (экспортируется через `lib/api`). Убедиться, что НИ ОДИН оставшийся файл не импортирует `NewsItem`/news-экшены из `../store`. Найти: `grep -rn "from './store'\|from '../store'" src | grep -i news` и `grep -rn "useStore().*news\|state.news\|addNews\|updateNews\|deleteNews" src`.
- [ ] Если `Admin.tsx` всё ещё содержит `renderNews`, `editingNews`, `NewsItem`-импорт из стора (случай, когда WT-0 не смёржен) — удалить их; вкладка news теперь рендерится через вынесенный `NewsTab`.
- [ ] Verify:
  - `grep -rn "news\|News" src/store.ts` → пусто (кроме, возможно, несвязанных совпадений — проверить глазами).
  - `npm run lint` → без ошибок.

---

## Task 11 — Ручная интеграционная проверка (нет тест-раннера на фронте)

**Steps**
- [ ] Backend: `npm run migration:run` (dev-БД) → `npm run start:dev`.
- [ ] Front: `npm run dev`.
- [ ] Проверить сценарии:
  - Публичная `/news` — грузит список с API (пустой при чистой БД).
  - Админ → вкладка «Лента новостей»: создать новость (title + image URL + HTML-контент) → появляется в таблице и на публичной `/news`.
  - Открыть `/news/:id` — статья отображается, HTML рендерится.
  - Редактировать → изменения видны после инвалидации.
  - Удалить → исчезает из списка.
  - Разлогиниться / роль USER — POST/PUT/DELETE отклоняются (403); GET работает.
- [ ] Зафиксировать результаты прогона в отчёте.

---

## Self-Review

- [ ] **Способ миграций верный:** таблица создаётся raw-SQL миграцией `1700000000020-CreateNews.ts`, а НЕ полагается на `synchronize` (он только в dev). Сущность добавлена в ОБА массива `entities` (`app.module.ts` + `config/data-source.ts`). Проверен фактический uuid-генератор (`uuid_generate_v4()` vs `gen_random_uuid()`) по существующей users-миграции.
- [ ] **RBAC корректен:** публичные GET помечены `@Public()` (иначе глобальный `JwtAuthGuard` их закроет); админ-маршруты — `@UseGuards(RolesGuard)` + `@Roles(ADMIN, MANAGER)`. Проверено, что `RolesGuard` читает `user.roles` (массив).
- [ ] **Контракт полей согласован:** entity `body/coverImage/publishedAt` ↔ фронт `content/image/date` — маппинг изолирован в `newsApi.ts` (`toItem`/`toBody`); публичные страницы и `NewsTab` используют фронт-форму `NewsItem`.
- [ ] **Backend-тест написан ПЕРВЫМ** (`news.service.spec.ts`), прошёл RED→GREEN; использован проектный паттерн (мок-репозиторий без TestingModule). `npm test` зелёный целиком.
- [ ] **Фронт:** данные только через `apiRequest` (импорт из `../../api` или `../../http` — по факту наличия `http.ts`); состояние — react-query; токен — `useAuthStore(s => s.accessToken)`. `npm run lint` зелёный.
- [ ] **localStorage-логика новостей удалена** из `store.ts`; не осталось импортов старого `NewsItem`/news-экшенов из стора (проверено grep'ом).
- [ ] **MERGE-POINT отмечен:** `src/app.module.ts` — единственный файл, общий с WT-6/WT-7; конфликт тривиальный (добавление строк в `imports`/`entities`).
- [ ] `src/lib/api/index.ts` НЕ изменялся (реэкспорт `newsApi` уже есть по WT-0).
- [ ] Ручная интеграционная проверка (Task 11) выполнена, все сценарии зелёные.
