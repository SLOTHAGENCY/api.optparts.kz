# «Мой гараж» (VIN-based garage) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the static "Мой гараж" profile tab into a real feature — users add/edit/delete VIN-anchored cars persisted in the DB, and jump to the parts catalog for a car.

**Architecture:** Backend is a NestJS module `src/garage/` cloned from the existing `src/addresses/` user-owned-collection pattern (entity + DTOs + service + controller + migration, protected by the global `JwtAuthGuard`, served under the global `api` prefix). Frontend adds a `garageApi` client and rewrites the garage tab in `front/src/pages/Profile.tsx` with real CRUD state.

**Tech Stack:** NestJS, TypeORM (PostgreSQL), class-validator, Jest (backend); React + Zustand + react-router + Tailwind (frontend).

## Global Constraints

- Backend repo: `/home/mans/projects/Dana/api.optparts.kz`. Frontend repo: `/home/mans/projects/Dana/front`.
- Global prefix `api` (`main.ts`): `@Controller('garage')` serves under `/api/garage`.
- Global `JwtAuthGuard` (`APP_GUARD` in `app.module.ts`) protects all routes by default — do NOT add `@UseGuards` and do NOT add `@Public()` to garage endpoints.
- Use `@CurrentUser()` from `../auth/decorators/current-user.decorator` to get the authenticated `User`.
- VIN is the only required field. Normalize VIN as `vin.trim().toUpperCase()` before persisting/comparing.
- Duplicate VIN for the *same* user → `ConflictException` (409) "Это авто уже в гараже." Different users may share a VIN.
- Migration filename must sort after the latest (`...023`): use `1700000000024-CreateVehicles.ts`.
- Prod runs a compiled build (`node dist/main`); per repo `CLAUDE.md`, source changes need `npm run build` + process restart to take effect.

---

### Task 1: Data layer — Vehicle entity, DTOs, migration

**Files:**
- Create: `src/garage/entities/vehicle.entity.ts`
- Create: `src/garage/dto/create-vehicle.dto.ts`
- Create: `src/garage/dto/update-vehicle.dto.ts`
- Create: `src/migrations/1700000000024-CreateVehicles.ts`

**Interfaces:**
- Consumes: `User` from `../../users/entities/user.entity`.
- Produces:
  - `Vehicle` entity with fields `id, vin, make, model, year, trim, main, userId, user, createdAt, updatedAt`.
  - `CreateVehicleDto { vin: string; make?: string; model?: string; year?: number; trim?: string; main?: boolean }`.
  - `UpdateVehicleDto` — same fields, all optional.

- [ ] **Step 1: Write the entity**

`src/garage/entities/vehicle.entity.ts`:
```ts
import {
  Entity, PrimaryGeneratedColumn, Column,
  ManyToOne, JoinColumn, CreateDateColumn, UpdateDateColumn,
} from 'typeorm';
import { User } from '../../users/entities/user.entity';

@Entity('vehicles')
export class Vehicle {
  @PrimaryGeneratedColumn('uuid')
  id: string;

  @Column({ length: 32 })
  vin: string;

  @Column({ length: 100, nullable: true, default: null })
  make: string | null;

  @Column({ length: 100, nullable: true, default: null })
  model: string | null;

  @Column({ type: 'int', nullable: true, default: null })
  year: number | null;

  @Column({ length: 120, nullable: true, default: null })
  trim: string | null;

  @Column({ default: false })
  main: boolean;

  @ManyToOne(() => User, { onDelete: 'CASCADE' })
  @JoinColumn()
  user: User;

  @Column()
  userId: string;

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;
}
```

- [ ] **Step 2: Write the create DTO**

`src/garage/dto/create-vehicle.dto.ts`:
```ts
import {
  IsString, IsNotEmpty, IsOptional, IsBoolean, IsInt,
  Min, Max, MinLength, MaxLength, Matches,
} from 'class-validator';

export class CreateVehicleDto {
  @IsString()
  @IsNotEmpty()
  @MinLength(5)
  @MaxLength(32)
  @Matches(/^[A-Za-z0-9]+$/, { message: 'VIN может содержать только буквы и цифры.' })
  vin: string;

  @IsOptional()
  @IsString()
  @MaxLength(100)
  make?: string;

  @IsOptional()
  @IsString()
  @MaxLength(100)
  model?: string;

  @IsOptional()
  @IsInt()
  @Min(1900)
  @Max(2100)
  year?: number;

  @IsOptional()
  @IsString()
  @MaxLength(120)
  trim?: string;

  @IsOptional()
  @IsBoolean()
  main?: boolean;
}
```

- [ ] **Step 3: Write the update DTO**

`src/garage/dto/update-vehicle.dto.ts`:
```ts
import {
  IsString, IsOptional, IsBoolean, IsInt,
  Min, Max, MinLength, MaxLength, Matches,
} from 'class-validator';

export class UpdateVehicleDto {
  @IsOptional()
  @IsString()
  @MinLength(5)
  @MaxLength(32)
  @Matches(/^[A-Za-z0-9]+$/, { message: 'VIN может содержать только буквы и цифры.' })
  vin?: string;

  @IsOptional()
  @IsString()
  @MaxLength(100)
  make?: string;

  @IsOptional()
  @IsString()
  @MaxLength(100)
  model?: string;

  @IsOptional()
  @IsInt()
  @Min(1900)
  @Max(2100)
  year?: number;

  @IsOptional()
  @IsString()
  @MaxLength(120)
  trim?: string;

  @IsOptional()
  @IsBoolean()
  main?: boolean;
}
```

- [ ] **Step 4: Write the migration**

`src/migrations/1700000000024-CreateVehicles.ts`:
```ts
import { MigrationInterface, QueryRunner } from 'typeorm';

export class CreateVehicles1700000000024 implements MigrationInterface {
  name = 'CreateVehicles1700000000024';

  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`
      CREATE TABLE "vehicles" (
        "id"        uuid          NOT NULL DEFAULT uuid_generate_v4(),
        "vin"       varchar(32)   NOT NULL,
        "make"      varchar(100),
        "model"     varchar(100),
        "year"      integer,
        "trim"      varchar(120),
        "main"      boolean       NOT NULL DEFAULT false,
        "userId"    uuid          NOT NULL,
        "createdAt" TIMESTAMP     NOT NULL DEFAULT now(),
        "updatedAt" TIMESTAMP     NOT NULL DEFAULT now(),
        CONSTRAINT "PK_vehicles" PRIMARY KEY ("id"),
        CONSTRAINT "FK_vehicles_users" FOREIGN KEY ("userId")
          REFERENCES "users"("id") ON DELETE CASCADE
      )
    `);
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`DROP TABLE "vehicles"`);
  }
}
```

- [ ] **Step 5: Verify it compiles**

Run: `cd /home/mans/projects/Dana/api.optparts.kz && npx tsc --noEmit -p tsconfig.json`
Expected: no errors referencing the new files. (Pre-existing unrelated errors, if any, are out of scope — confirm none mention `garage/` or `1700000000024`.)

- [ ] **Step 6: Commit**

```bash
cd /home/mans/projects/Dana/api.optparts.kz
git add src/garage/entities/vehicle.entity.ts src/garage/dto/create-vehicle.dto.ts src/garage/dto/update-vehicle.dto.ts src/migrations/1700000000024-CreateVehicles.ts
git commit -m "feat(garage): add Vehicle entity, DTOs, and migration"
```

---

### Task 2: GarageService (TDD)

**Files:**
- Create: `src/garage/garage.service.ts`
- Test: `src/garage/garage.service.spec.ts`

**Interfaces:**
- Consumes: `Vehicle` (Task 1), `CreateVehicleDto`, `UpdateVehicleDto` (Task 1).
- Produces `GarageService` with:
  - `findAllByUser(userId: string): Promise<Vehicle[]>`
  - `findOne(id: string, userId: string): Promise<Vehicle>`
  - `create(userId: string, dto: CreateVehicleDto): Promise<Vehicle>`
  - `update(id: string, userId: string, dto: UpdateVehicleDto): Promise<Vehicle>`
  - `setMain(id: string, userId: string): Promise<Vehicle>`
  - `delete(id: string, userId: string): Promise<void>`

- [ ] **Step 1: Write the failing tests**

`src/garage/garage.service.spec.ts` (plain-jest + mocked repo, matching `cart.service.spec.ts` style):
```ts
import { ConflictException, ForbiddenException, NotFoundException } from '@nestjs/common';
import { GarageService } from './garage.service';
import { Vehicle } from './entities/vehicle.entity';

function makeVehicle(partial: Partial<Vehicle> = {}): Vehicle {
  return {
    id: 'v1',
    vin: 'JHME5000000000001',
    make: null,
    model: null,
    year: null,
    trim: null,
    main: false,
    userId: 'u1',
    user: undefined as any,
    createdAt: new Date(0),
    updatedAt: new Date(0),
    ...partial,
  };
}

function makeService(seed: Vehicle[] = []) {
  const store = [...seed];
  const repo = {
    find: jest.fn(async ({ where }: any) =>
      store.filter((v) => v.userId === where.userId),
    ),
    findOne: jest.fn(async ({ where }: any) =>
      store.find((v) => v.id === where.id) ?? null,
    ),
    create: jest.fn((d: any) => makeVehicle({ id: 'new', ...d })),
    save: jest.fn(async (v: Vehicle) => {
      const idx = store.findIndex((s) => s.id === v.id);
      if (idx >= 0) store[idx] = v;
      else store.push(v);
      return v;
    }),
    remove: jest.fn(async (v: Vehicle) => {
      const idx = store.findIndex((s) => s.id === v.id);
      if (idx >= 0) store.splice(idx, 1);
      return v;
    }),
    update: jest.fn(async (criteria: any, patch: any) => {
      store
        .filter((v) => v.userId === criteria.userId && v.main === criteria.main)
        .forEach((v) => Object.assign(v, patch));
      return { affected: 0 };
    }),
  };
  const service = new GarageService(repo as any);
  return { service, repo, store };
}

describe('GarageService', () => {
  it('normalizes VIN to trimmed uppercase on create', async () => {
    const { service } = makeService();
    const created = await service.create('u1', { vin: '  jhmes000000000abc ' });
    expect(created.vin).toBe('JHMES000000000ABC');
  });

  it('rejects a duplicate VIN for the same user with 409', async () => {
    const { service } = makeService([makeVehicle({ vin: 'JHMES000000000ABC' })]);
    await expect(
      service.create('u1', { vin: 'jhmes000000000abc' }),
    ).rejects.toBeInstanceOf(ConflictException);
  });

  it('allows the same VIN for a different user', async () => {
    const { service } = makeService([makeVehicle({ vin: 'JHMES000000000ABC', userId: 'u2' })]);
    const created = await service.create('u1', { vin: 'JHMES000000000ABC' });
    expect(created.userId).toBe('u1');
  });

  it('unsets other mains when creating a main vehicle', async () => {
    const { service, store } = makeService([makeVehicle({ id: 'v1', main: true })]);
    await service.create('u1', { vin: 'NEWVIN0000001', main: true });
    expect(store.find((v) => v.id === 'v1')!.main).toBe(false);
  });

  it('findOne throws 404 when missing', async () => {
    const { service } = makeService();
    await expect(service.findOne('nope', 'u1')).rejects.toBeInstanceOf(NotFoundException);
  });

  it('findOne throws 403 when owned by another user', async () => {
    const { service } = makeService([makeVehicle({ id: 'v1', userId: 'u2' })]);
    await expect(service.findOne('v1', 'u1')).rejects.toBeInstanceOf(ForbiddenException);
  });

  it('setMain unsets others and sets the target', async () => {
    const { service, store } = makeService([
      makeVehicle({ id: 'v1', main: true }),
      makeVehicle({ id: 'v2', vin: 'SECONDVIN0001', main: false }),
    ]);
    const result = await service.setMain('v2', 'u1');
    expect(result.main).toBe(true);
    expect(store.find((v) => v.id === 'v1')!.main).toBe(false);
  });

  it('delete removes an owned vehicle', async () => {
    const { service, store } = makeService([makeVehicle({ id: 'v1' })]);
    await service.delete('v1', 'u1');
    expect(store).toHaveLength(0);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /home/mans/projects/Dana/api.optparts.kz && npx jest src/garage/garage.service.spec.ts`
Expected: FAIL — cannot find module `./garage.service`.

- [ ] **Step 3: Write the service**

`src/garage/garage.service.ts`:
```ts
import {
  Injectable, NotFoundException, ForbiddenException, ConflictException,
} from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Vehicle } from './entities/vehicle.entity';
import { CreateVehicleDto } from './dto/create-vehicle.dto';
import { UpdateVehicleDto } from './dto/update-vehicle.dto';

@Injectable()
export class GarageService {
  constructor(
    @InjectRepository(Vehicle)
    private readonly repo: Repository<Vehicle>,
  ) {}

  private normalizeVin(vin: string): string {
    return vin.trim().toUpperCase();
  }

  findAllByUser(userId: string): Promise<Vehicle[]> {
    return this.repo.find({
      where: { userId },
      order: { main: 'DESC', createdAt: 'ASC' },
    });
  }

  async findOne(id: string, userId: string): Promise<Vehicle> {
    const vehicle = await this.repo.findOne({ where: { id } });
    if (!vehicle) throw new NotFoundException('Vehicle not found.');
    if (vehicle.userId !== userId) throw new ForbiddenException('Access denied.');
    return vehicle;
  }

  async create(userId: string, dto: CreateVehicleDto): Promise<Vehicle> {
    const vin = this.normalizeVin(dto.vin);
    const existing = await this.repo.findOne({ where: { userId, vin } });
    if (existing) throw new ConflictException('Это авто уже в гараже.');

    if (dto.main) {
      await this.unsetMain(userId);
    }
    const vehicle = this.repo.create({ ...dto, vin, userId });
    return this.repo.save(vehicle);
  }

  async update(id: string, userId: string, dto: UpdateVehicleDto): Promise<Vehicle> {
    const vehicle = await this.findOne(id, userId);
    if (dto.main === true) {
      await this.unsetMain(userId);
    }
    const patch: Partial<Vehicle> = { ...dto };
    if (dto.vin !== undefined) {
      patch.vin = this.normalizeVin(dto.vin);
    }
    Object.assign(vehicle, patch);
    return this.repo.save(vehicle);
  }

  async setMain(id: string, userId: string): Promise<Vehicle> {
    const vehicle = await this.findOne(id, userId);
    await this.unsetMain(userId);
    vehicle.main = true;
    return this.repo.save(vehicle);
  }

  async delete(id: string, userId: string): Promise<void> {
    const vehicle = await this.findOne(id, userId);
    await this.repo.remove(vehicle);
  }

  private async unsetMain(userId: string): Promise<void> {
    await this.repo.update({ userId, main: true }, { main: false });
  }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /home/mans/projects/Dana/api.optparts.kz && npx jest src/garage/garage.service.spec.ts`
Expected: PASS — 8 passing.

- [ ] **Step 5: Commit**

```bash
cd /home/mans/projects/Dana/api.optparts.kz
git add src/garage/garage.service.ts src/garage/garage.service.spec.ts
git commit -m "feat(garage): add GarageService with unit tests"
```

---

### Task 3: GarageController + module + registration

**Files:**
- Create: `src/garage/garage.controller.ts`
- Create: `src/garage/garage.module.ts`
- Modify: `src/app.module.ts` (import + register `GarageModule` and `Vehicle` entity)

**Interfaces:**
- Consumes: `GarageService` (Task 2), `CreateVehicleDto`/`UpdateVehicleDto` (Task 1), `Vehicle` (Task 1), `CurrentUser` decorator, `User` entity.
- Produces: HTTP endpoints `GET /api/garage`, `GET /api/garage/:id`, `POST /api/garage`, `PUT /api/garage/:id`, `PATCH /api/garage/:id/main`, `DELETE /api/garage/:id`.

- [ ] **Step 1: Write the controller**

`src/garage/garage.controller.ts`:
```ts
import {
  Controller, Get, Post, Put, Patch,
  Delete, Body, Param, HttpCode, HttpStatus, ParseUUIDPipe,
} from '@nestjs/common';
import {
  ApiTags, ApiBearerAuth, ApiOperation, ApiParam, ApiResponse,
} from '@nestjs/swagger';
import { GarageService } from './garage.service';
import { CreateVehicleDto } from './dto/create-vehicle.dto';
import { UpdateVehicleDto } from './dto/update-vehicle.dto';
import { CurrentUser } from '../auth/decorators/current-user.decorator';
import { User } from '../users/entities/user.entity';

@ApiTags('garage')
@ApiBearerAuth()
@Controller('garage')
export class GarageController {
  constructor(private readonly garageService: GarageService) {}

  @Get()
  @ApiOperation({
    summary: 'Список авто в гараже пользователя',
    description:
      'Возвращает все сохранённые автомобили текущего пользователя (его «гараж»). ' +
      'Каждый пользователь видит только свои авто. Требует авторизации.',
  })
  findAll(@CurrentUser() user: User) {
    return this.garageService.findAllByUser(user.id);
  }

  @Get(':id')
  @ApiOperation({
    summary: 'Получить одно авто по id',
    description:
      'Возвращает данные одного автомобиля из гаража по его id. Если авто не найдено ' +
      '(или принадлежит другому пользователю) — вернётся ошибка. Требует авторизации.',
  })
  @ApiParam({ name: 'id', format: 'uuid' })
  @ApiResponse({ status: 404, description: 'Авто не найдено.' })
  findOne(@CurrentUser() user: User, @Param('id', ParseUUIDPipe) id: string) {
    return this.garageService.findOne(id, user.id);
  }

  @Post()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Добавить авто в гараж',
    description:
      'Сохраняет новый автомобиль в гараж текущего пользователя. Обязателен только VIN; ' +
      'марка, модель, год и комплектация — по желанию. Требует авторизации.',
  })
  @ApiResponse({ status: 409, description: 'Это авто уже в гараже.' })
  create(@CurrentUser() user: User, @Body() dto: CreateVehicleDto) {
    return this.garageService.create(user.id, dto);
  }

  @Put(':id')
  @ApiOperation({
    summary: 'Изменить авто в гараже',
    description:
      'Обновляет данные автомобиля по его id. Менять можно только свои авто. Требует авторизации.',
  })
  @ApiParam({ name: 'id', format: 'uuid' })
  update(
    @CurrentUser() user: User,
    @Param('id', ParseUUIDPipe) id: string,
    @Body() dto: UpdateVehicleDto,
  ) {
    return this.garageService.update(id, user.id, dto);
  }

  @Patch(':id/main')
  @ApiOperation({
    summary: 'Сделать авто основным',
    description:
      'Помечает выбранное авто как основное. Основным может быть только одно авто: ' +
      'предыдущее основное перестаёт быть таковым. Требует авторизации.',
  })
  @ApiParam({ name: 'id', format: 'uuid' })
  setMain(@CurrentUser() user: User, @Param('id', ParseUUIDPipe) id: string) {
    return this.garageService.setMain(id, user.id);
  }

  @Delete(':id')
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({
    summary: 'Удалить авто из гаража',
    description: 'Удаляет автомобиль по его id. Удалить можно только своё авто. Требует авторизации.',
  })
  @ApiParam({ name: 'id', format: 'uuid' })
  delete(@CurrentUser() user: User, @Param('id', ParseUUIDPipe) id: string) {
    return this.garageService.delete(id, user.id);
  }
}
```

- [ ] **Step 2: Write the module**

`src/garage/garage.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { Vehicle } from './entities/vehicle.entity';
import { GarageService } from './garage.service';
import { GarageController } from './garage.controller';

@Module({
  imports: [TypeOrmModule.forFeature([Vehicle])],
  providers: [GarageService],
  controllers: [GarageController],
  exports: [GarageService],
})
export class GarageModule {}
```

- [ ] **Step 3: Register in app.module.ts**

In `src/app.module.ts`, next to the `AddressesModule` / `Address` lines:
- Add imports near the other module imports:
  ```ts
  import { GarageModule } from './garage/garage.module';
  import { Vehicle } from './garage/entities/vehicle.entity';
  ```
- Add `Vehicle` to the TypeORM `entities` array (wherever `Address` is listed in that array).
- Add `GarageModule` to the `imports` array of `@Module` (next to `AddressesModule`).

Verify both `Address`/`AddressesModule` occurrences to mirror them: run
`grep -n "Address" src/app.module.ts` first and place `Vehicle`/`GarageModule` in the exact same two lists.

- [ ] **Step 4: Build to verify wiring**

Run: `cd /home/mans/projects/Dana/api.optparts.kz && npm run build`
Expected: build succeeds, `dist/garage/` produced.

- [ ] **Step 5: Restart and smoke-test the endpoint**

Per repo `CLAUDE.md` restart flow:
```bash
cd /home/mans/projects/Dana/api.optparts.kz
ss -ltnp | grep ':3000'          # find pid holding :3000
kill <pid>                        # stop old instance
setsid nohup node dist/main > /tmp/optparts-api.log 2>&1 < /dev/null &
disown
sleep 2
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/api/garage
```
Expected: `401` (endpoint exists, guard rejects anonymous request — confirms it is wired and protected). A `404` would mean the route is not registered.

- [ ] **Step 6: Commit**

```bash
cd /home/mans/projects/Dana/api.optparts.kz
git add src/garage/garage.controller.ts src/garage/garage.module.ts src/app.module.ts
git commit -m "feat(garage): add controller, module, and register in AppModule"
```

---

### Task 4: Frontend API — Vehicle type + garageApi

**Files:**
- Modify: `front/src/types/catalog.ts` (add `Vehicle` interface)
- Modify: `front/src/api.ts` (add `garageApi`)

**Interfaces:**
- Consumes: `apiRequest` helper already in `front/src/api.ts`.
- Produces:
  - `Vehicle` type (import target for Profile).
  - `garageApi` with `list`, `create`, `update`, `setMain`, `delete`.

- [ ] **Step 1: Add the Vehicle type**

Append to `front/src/types/catalog.ts` (near the `Address` interface):
```ts
export interface Vehicle {
  id: string;
  vin: string;
  make: string | null;
  model: string | null;
  year: number | null;
  trim: string | null;
  main: boolean;
  createdAt: string;
  updatedAt: string;
}
```

- [ ] **Step 2: Add garageApi**

In `front/src/api.ts`, immediately after the `addressesApi` block. First confirm `Vehicle` is
importable in the `import type { ... }` block at the top (add `Vehicle` to the existing
`from './types/catalog'` type-import list; check the top of the file — the `Address` type is
already imported there, add `Vehicle` alongside it):
```ts
export interface VehicleInput {
  vin: string;
  make?: string;
  model?: string;
  year?: number;
  trim?: string;
  main?: boolean;
}

export const garageApi = {
  list: (token: string) => apiRequest<Vehicle[]>('/api/garage', { token }),
  create: (token: string, body: VehicleInput) =>
    apiRequest<Vehicle>('/api/garage', { method: 'POST', token, body }),
  update: (token: string, id: string, body: VehicleInput) =>
    apiRequest<Vehicle>(`/api/garage/${id}`, { method: 'PUT', token, body }),
  setMain: (token: string, id: string) =>
    apiRequest<Vehicle>(`/api/garage/${id}/main`, { method: 'PATCH', token }),
  delete: (token: string, id: string) =>
    apiRequest<void>(`/api/garage/${id}`, { method: 'DELETE', token }),
};
```

- [ ] **Step 3: Typecheck the frontend**

Run: `cd /home/mans/projects/Dana/front && npx tsc --noEmit`
Expected: no new errors mentioning `garageApi`, `Vehicle`, or `api.ts`.

- [ ] **Step 4: Commit**

```bash
cd /home/mans/projects/Dana/front
git add src/types/catalog.ts src/api.ts
git commit -m "feat(garage): add Vehicle type and garageApi client"
```

---

### Task 5: Profile.tsx — real garage tab UI

**Files:**
- Modify: `front/src/pages/Profile.tsx`

**Interfaces:**
- Consumes: `garageApi`, `VehicleInput` (Task 4), `Vehicle` type (Task 4), existing
  `accessToken`, `getApiErrorMessage`, `isUnauthorizedError`, `navigate`.
- Produces: interactive garage tab (no exported interface).

- [ ] **Step 1: Add imports**

At the top of `front/src/pages/Profile.tsx`:
- Extend the lucide-react import to include `Trash2` and `Pencil`:
  ```ts
  import { Package, Heart, MapPin, Settings, LogOut, Car, Shield, Camera, Plus, X, Trash2, Pencil } from 'lucide-react';
  ```
- Add API + type imports (near the existing `../api` and store imports):
  ```ts
  import { garageApi, type VehicleInput } from '../api';
  import type { Vehicle } from '../types/catalog';
  ```
- Add `useCallback` to the React import:
  ```ts
  import React, { useCallback, useEffect, useMemo, useState } from 'react';
  ```

- [ ] **Step 2: Add garage state, loader, and handlers**

Inside the `Profile` component, after the existing `useState` declarations (e.g. after
`isLoggingOut`), add:
```tsx
  const [vehicles, setVehicles] = useState<Vehicle[]>([]);
  const [garageError, setGarageError] = useState<string | null>(null);
  const [showVehicleForm, setShowVehicleForm] = useState(false);
  const [editingVehicleId, setEditingVehicleId] = useState<string | null>(null);
  const [vehicleForm, setVehicleForm] = useState({
    vin: '', make: '', model: '', year: '', trim: '',
  });
  const [isVehicleSaving, setIsVehicleSaving] = useState(false);

  const vehicleTitle = (v: Vehicle): string =>
    [v.year, v.make, v.model].filter(Boolean).join(' ') || v.vin;

  const loadVehicles = useCallback(async () => {
    if (!accessToken) return;
    try {
      const list = await garageApi.list(accessToken);
      setVehicles(list);
      setGarageError(null);
    } catch (error) {
      if (isUnauthorizedError(error)) {
        navigate('/auth', { replace: true });
        return;
      }
      setGarageError(getApiErrorMessage(error));
    }
  }, [accessToken, navigate]);

  useEffect(() => {
    void loadVehicles();
  }, [loadVehicles]);

  const resetVehicleForm = () => {
    setVehicleForm({ vin: '', make: '', model: '', year: '', trim: '' });
    setEditingVehicleId(null);
    setShowVehicleForm(false);
  };

  const openAddVehicle = () => {
    setVehicleForm({ vin: '', make: '', model: '', year: '', trim: '' });
    setEditingVehicleId(null);
    setShowVehicleForm(true);
    setGarageError(null);
  };

  const openEditVehicle = (v: Vehicle) => {
    setVehicleForm({
      vin: v.vin,
      make: v.make || '',
      model: v.model || '',
      year: v.year ? String(v.year) : '',
      trim: v.trim || '',
    });
    setEditingVehicleId(v.id);
    setShowVehicleForm(true);
    setGarageError(null);
  };

  const handleVehicleFormChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const { name, value } = e.target;
    setVehicleForm((current) => ({
      ...current,
      [name]: name === 'vin' ? value.toUpperCase() : value,
    }));
  };

  const handleVehicleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!accessToken) return;
    setGarageError(null);
    setIsVehicleSaving(true);

    const payload: VehicleInput = { vin: vehicleForm.vin.trim() };
    if (vehicleForm.make.trim()) payload.make = vehicleForm.make.trim();
    if (vehicleForm.model.trim()) payload.model = vehicleForm.model.trim();
    if (vehicleForm.trim.trim()) payload.trim = vehicleForm.trim.trim();
    const yearNum = parseInt(vehicleForm.year, 10);
    if (!Number.isNaN(yearNum)) payload.year = yearNum;

    try {
      if (editingVehicleId) {
        await garageApi.update(accessToken, editingVehicleId, payload);
      } else {
        await garageApi.create(accessToken, payload);
      }
      resetVehicleForm();
      await loadVehicles();
    } catch (error) {
      if (isUnauthorizedError(error)) {
        navigate('/auth', { replace: true });
        return;
      }
      setGarageError(getApiErrorMessage(error));
    } finally {
      setIsVehicleSaving(false);
    }
  };

  const handleVehicleDelete = async (id: string) => {
    if (!accessToken) return;
    setGarageError(null);
    try {
      await garageApi.delete(accessToken, id);
      await loadVehicles();
    } catch (error) {
      if (isUnauthorizedError(error)) {
        navigate('/auth', { replace: true });
        return;
      }
      setGarageError(getApiErrorMessage(error));
    }
  };

  const handleVehicleSetMain = async (id: string) => {
    if (!accessToken) return;
    setGarageError(null);
    try {
      await garageApi.setMain(accessToken, id);
      await loadVehicles();
    } catch (error) {
      if (isUnauthorizedError(error)) {
        navigate('/auth', { replace: true });
        return;
      }
      setGarageError(getApiErrorMessage(error));
    }
  };
```

- [ ] **Step 3: Replace the hardcoded garage block**

Replace the entire `{activeTab === 'garage' && ( ... )}` block (currently lines ~266–299,
the one containing "2018 Honda Civic" / "2015 Ford F-150") with:
```tsx
              {activeTab === 'garage' && (
                <div>
                  <div className="flex justify-between items-center mb-6">
                    <h2 className="text-[18px] font-bold text-slate-900 border-l-4 border-orange-500 pl-3">Мой гараж</h2>
                    <button
                      onClick={openAddVehicle}
                      className="text-[13px] font-bold text-orange-500 hover:underline"
                    >
                      + Добавить авто
                    </button>
                  </div>
                  <p className="text-[14px] text-slate-500 mb-6">Сохраните свои автомобили, чтобы быстро находить подходящие запчасти.</p>

                  {garageError && (
                    <div className="mb-4 rounded border border-red-200 bg-red-50 px-4 py-3 text-[13px] text-red-700 whitespace-pre-line">
                      {garageError}
                    </div>
                  )}

                  {showVehicleForm && (
                    <form onSubmit={handleVehicleSubmit} className="mb-6 border border-slate-200 rounded p-4 space-y-3">
                      <div>
                        <label className="block text-[13px] font-bold text-slate-700 mb-1">VIN *</label>
                        <input
                          required
                          name="vin"
                          value={vehicleForm.vin}
                          onChange={handleVehicleFormChange}
                          disabled={isVehicleSaving}
                          placeholder="Например: WVWZZZ1KZAW000000"
                          className="w-full border border-slate-200 rounded p-2.5 outline-none focus:border-orange-500 text-[14px] disabled:bg-slate-50"
                        />
                      </div>
                      <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
                        <div>
                          <label className="block text-[13px] font-bold text-slate-700 mb-1">Марка</label>
                          <input
                            name="make"
                            value={vehicleForm.make}
                            onChange={handleVehicleFormChange}
                            disabled={isVehicleSaving}
                            placeholder="Honda"
                            className="w-full border border-slate-200 rounded p-2.5 outline-none focus:border-orange-500 text-[14px] disabled:bg-slate-50"
                          />
                        </div>
                        <div>
                          <label className="block text-[13px] font-bold text-slate-700 mb-1">Модель</label>
                          <input
                            name="model"
                            value={vehicleForm.model}
                            onChange={handleVehicleFormChange}
                            disabled={isVehicleSaving}
                            placeholder="Civic"
                            className="w-full border border-slate-200 rounded p-2.5 outline-none focus:border-orange-500 text-[14px] disabled:bg-slate-50"
                          />
                        </div>
                        <div>
                          <label className="block text-[13px] font-bold text-slate-700 mb-1">Год</label>
                          <input
                            name="year"
                            inputMode="numeric"
                            value={vehicleForm.year}
                            onChange={handleVehicleFormChange}
                            disabled={isVehicleSaving}
                            placeholder="2018"
                            className="w-full border border-slate-200 rounded p-2.5 outline-none focus:border-orange-500 text-[14px] disabled:bg-slate-50"
                          />
                        </div>
                        <div>
                          <label className="block text-[13px] font-bold text-slate-700 mb-1">Комплектация / двигатель</label>
                          <input
                            name="trim"
                            value={vehicleForm.trim}
                            onChange={handleVehicleFormChange}
                            disabled={isVehicleSaving}
                            placeholder="EX-L 1.5L Turbo"
                            className="w-full border border-slate-200 rounded p-2.5 outline-none focus:border-orange-500 text-[14px] disabled:bg-slate-50"
                          />
                        </div>
                      </div>
                      <div className="flex gap-3 pt-1">
                        <button
                          type="submit"
                          disabled={isVehicleSaving || !vehicleForm.vin.trim()}
                          className="bg-slate-900 hover:bg-slate-800 disabled:bg-slate-400 text-white font-bold py-2.5 px-6 rounded transition-colors text-[13px] uppercase"
                        >
                          {isVehicleSaving ? 'Сохранение...' : editingVehicleId ? 'Сохранить' : 'Добавить'}
                        </button>
                        <button
                          type="button"
                          onClick={resetVehicleForm}
                          disabled={isVehicleSaving}
                          className="text-[13px] font-bold text-slate-500 hover:text-slate-900 px-4"
                        >
                          Отмена
                        </button>
                      </div>
                    </form>
                  )}

                  {vehicles.length === 0 && !showVehicleForm ? (
                    <div className="text-slate-500 py-8 text-center text-[14px]">
                      Гараж пуст — добавьте авто, чтобы быстрее находить запчасти.
                    </div>
                  ) : (
                    <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                      {vehicles.map((v) => (
                        <div
                          key={v.id}
                          className={`rounded p-4 relative transition-colors ${v.main ? 'border-2 border-orange-500 bg-orange-50/30' : 'border border-slate-200 hover:border-slate-300'}`}
                        >
                          {v.main && (
                            <div className="absolute top-4 right-4 bg-orange-500 text-white text-[10px] uppercase tracking-wider font-bold px-2 py-1 rounded">Основной</div>
                          )}
                          <Car className={`mb-3 ${v.main ? 'text-orange-500' : 'text-slate-400'}`} size={32} />
                          <h3 className="font-bold text-slate-900 text-[16px]">{vehicleTitle(v)}</h3>
                          <p className="text-[13px] text-slate-500 mb-1">{v.trim || 'VIN'}: {v.vin}</p>
                          <div className="flex flex-wrap gap-3 mt-3">
                            <button
                              onClick={() => navigate(`/vin/${encodeURIComponent(v.vin)}`)}
                              className="text-[13px] font-bold text-orange-500 hover:underline"
                            >
                              Каталог запчастей
                            </button>
                            {!v.main && (
                              <button
                                onClick={() => handleVehicleSetMain(v.id)}
                                className="text-[13px] font-bold text-slate-500 hover:text-slate-900"
                              >
                                Сделать основным
                              </button>
                            )}
                            <button
                              onClick={() => openEditVehicle(v)}
                              className="inline-flex items-center gap-1 text-[13px] font-bold text-slate-500 hover:text-slate-900"
                            >
                              <Pencil size={14} /> Изменить
                            </button>
                            <button
                              onClick={() => handleVehicleDelete(v.id)}
                              className="inline-flex items-center gap-1 text-[13px] font-bold text-slate-500 hover:text-red-600"
                            >
                              <Trash2 size={14} /> Удалить
                            </button>
                          </div>
                        </div>
                      ))}
                    </div>
                  )}
                </div>
              )}
```

- [ ] **Step 4: Typecheck**

Run: `cd /home/mans/projects/Dana/front && npx tsc --noEmit`
Expected: no errors in `Profile.tsx`.

- [ ] **Step 5: Manual verification**

Use the `verify` skill (or manually): log in on the frontend, open Profile → «Мой гараж»:
- empty state shows when no cars;
- «+ Добавить авто» → fill VIN only → «Добавить» → card appears with VIN as title;
- add a second car with make/model/year → title reads e.g. "2018 Honda Civic";
- «Сделать основным» moves the "Основной" badge; only one card is main;
- «Изменить» edits; «Удалить» removes;
- adding a duplicate VIN shows the "Это авто уже в гараже." error;
- «Каталог запчастей» navigates to `/vin/<vin>`.

- [ ] **Step 6: Commit**

```bash
cd /home/mans/projects/Dana/front
git add src/pages/Profile.tsx
git commit -m "feat(garage): wire real garage CRUD into profile tab"
```

---

## Self-Review notes

- **Spec coverage:** entity/columns → Task 1; service logic incl. VIN normalization, dedupe 409,
  main bookkeeping, ownership 404/403 → Task 2; endpoints + guard-by-default → Task 3; frontend
  type + client → Task 4; real garage UI incl. `/vin/:q` deep-link, empty state, errors → Task 5.
  Out-of-scope items (VIN decode, order linking, addresses rework) intentionally absent.
- **Type consistency:** `garageApi` method names (`list/create/update/setMain/delete`) and
  `VehicleInput` shape are used identically in Task 4 (definition) and Task 5 (consumption);
  `Vehicle` fields match the entity in Task 1 and the type in Task 4.
- **Verification per task:** tsc/jest/build/curl smoke + a manual UI pass in the final task.
